[user]
    name = Diego Romero
    email = diego.romero@gusto.com
    signingkey = 75C52BBF1189578C
[core]
    editor = nvim
    pager = less -FRX
    excludesfile = ~/.gitignore
[gist]
    private = yes
[github]
    user = diego-romero-gusto
[alias]
    b = branch
    ci = commit
    cia = commit -a
    cim = commit -m
    cima = commit -a -m
    civ = commit -v
    civa = commit -v -a
    co = checkout
    cp = cherry-pick
    d = diff
    dc = diff --cached
    diffc = diff --cached
    dsb = delete-squashed-branches
    forkup = pull --rebase upstream
    h = stash
    hs = stash save
    last = log -1
    po = push origin
    pf = push --force-with-lease
    quicklog = log --oneline --decorate -10 --pretty=format:'%C(yellow)%h%C(reset)%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    quicklog-long = log --oneline --decorate --pretty=format:'%C(yellow)%h%C(reset)%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    r = remote
    rs = restore
    rewind-one = reset --hard HEAD^
    st = status
    sta = stash
    sw = switch
    swc = switch -c
    today = diff --stat 'HEAD@{midnight}'
    tree = log --graph --oneline --all
    undo = reset --mixed HEAD^
    unstage = reset HEAD
    up = pull --rebase origin
    yesterday = diff --stat 'HEAD@{yesterday}' 'HEAD@{midnight}'
[color]
    diff = auto
    status = auto
    branch = auto
    ui = true
[color "diff"]
    meta = blue
    frag = black
    old = red
    new = green
[color "status"]
    added = green
    changed = yellow
    untracked = cyan
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[branch]
    autosetuprebase = always
[log]
    decorate = short
[merge]
    conflictstyle = diff3
    renamelimit = 3000
[push]
    default = current
[pull]
    rebase = true
[commit]
    gpgsign = true
[gpg]
    program = /usr/local/bin/gpg
[feature]
    manyFiles = true
[fetch]
   prune = true
