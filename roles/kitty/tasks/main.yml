---

- name: Create kitty configuration directory
  ansible.builtin.file:
    path: "{{ ansible_facts.env.HOME }}/.config"
    recurse: true
    state: directory
  tags: [kitty]

- name: Symlink kitty configuration directory
  ansible.builtin.file:
    src: "{{ ansible_facts.env.PWD }}/roles/kitty/files/kitty"
    dest: "{{ ansible_facts.env.HOME }}/.config/kitty"
    state: link
  tags: [kitty]
