- 2026-06-04 [dotfiles] pair-flow ships ~13 new bash scripts but repo has no shell test harness (no bats/shellspec); polish Agent-resolvable bucket is unreachable for shell-logic findings. Consider adding bats-core + a mise `test` task.
- 2026-06-04 [dotfiles] decouple review/pairing commands from pair-flow (config mis-homed in pair-flow.yml; employer org names leak in panel-review.md:23; CLAUDE.md over-couples them). Full scope in `decouple-review-tooling.md`.
- 2026-06-15 [dotfiles] planwright-integration retires the migrated slash-command files and installs planwright skills (repo-class-free act-then-review gate per planwright's finding-categorization/gate-wiring doctrine), but tracked roles/osx/files/CLAUDE.md still documents the superseded pair-flow model (repo-class solo/multi-reviewer split, the migrated commands described as commands not skills, pair-flow-config.sh repo-class as the autonomy gate). The doc-model migration is spec-tracked-deferred (tasks.md Deferred: orchestrate/execution-skill retirement gated on planwright proving), but CLAUDE.md's Review-Workflows + Spec-Driven-Autonomy-Pipeline sections need a follow-up rewrite to planwright's model; seed for that doc-migration PR/spec.
- 2026-06-15 [dotfiles] after planwright-integration deleted the 6 migrated command files, roles/osx/files/claude/scripts/skill-contracts.sh still lists them (execute-task/polish/orchestrate/resume/spec-kickoff/spec-draft) across its loops; each check is guarded by `[ -f "$CMDS/$f" ]` so the deleted ones silently skip (lefthook stays green), but three loops (D-32 branch-naming subset, D-33 Active-gate, D-36 kickoff-brief) are now fully dead and the contract checks no longer protect the migrated skills (planwright owns those via its D-32 self-hosting guards now). Prune the dead entries as part of the deferred tooling/doc migration; touching this file is a lefthook/CI-config change so bundle it deliberately.
- 2026-06-15 [dotfiles] the ~/dev/planwright sibling clone is an undocumented bootstrap prerequisite: the "Install planwright framework via its writer" task in roles/osx/tasks/osx.yml is gated `when: ... is exists`, so on a fresh machine without planwright cloned the task silently no-ops and the user gets dotfiles with no planwright skills and no warning. The dependency lives only in that task's inline comment; spec Deferred ("planwright name-locked at ~/dev/planwright") assumes a manual sibling clone but nothing in README/specs documents it. Decide the bootstrap model (document the manual prereq / fail loudly when absent / automate the clone) and surface it where setup is discoverable.
- 2026-06-15 [dotfiles] cross-repo latent hazard: planwright scripts/install.sh `cp`s shipped commands into $CLAUDE_DIR/commands/, but dotfiles symlinks ~/.claude/commands -> roles/osx/files/claude/commands (a directory symlink), so if planwright ever ships a commands/ dir the writer would copy files into the tracked dotfiles source tree and pollute the repo on every `mise run osx`. Inert today (planwright ships skills/ only, no commands/), and the fix belongs in planwright (write commands through a path that resolves the symlink, or never target a consumer-managed dir); noting here so the dotfiles side re-checks when planwright adds commands.
- 2026-06-23 [dotfiles] planwright went public and dotfiles now installs it as a Claude Code plugin (marketplace add + plugin install in osx.yml; marketplace/plugin update in upgrade.yml), retiring the ~/dev/planwright sibling clone and scripts/install.sh writer. This resolves obs 2026-06-15 #5 (writer task silent no-op when uncloned) and #6 (writer cp-into-symlink pollution hazard, now moot), and makes specs/pair-flow/tasks.md:58 ("private" repo, "name-locked at ~/dev/planwright") plus the Deferred sibling-clone assumption stale; reconcile/close those records.
- 2026-06-24 [dotfiles] no shell linter in the pre-commit gate or on the host: lefthook.yml runs yamllint/ansible-lint/syntax-check/skill-contracts but no shellcheck, and shellcheck is not installed, yet the repo ships substantial bash (roles/osx/files/claude/scripts/*.sh, scripts/*.sh, the inbox-*/pair-flow-config/worktree-bootstrap family). Tool-grounded review of shell changes (e.g. the skill-contracts.sh trim in this branch) falls back to `bash -n` + judgment. Consider adding shellcheck to mise + a lefthook glob on *.sh; seed for a tooling-hardening PR.
- 2026-07-13 [dotfiles] resolves obs 2026-06-15 #3's repo-class portion: `repo-class`/`pair-flow-config.sh`/`pair-flow.yml` removed entirely from `roles/osx/files/CLAUDE.md`, `panel-review.md`, `panel-pairing.md`, and `peer-review.md`. User's reasoning on removal: every consumer (`/panel-pairing`, `/peer-review`, planwright's `/polish`) already drafts a PR and never marks it ready without human sign-off regardless of solo vs multi-reviewer, so the repo-class gate wasn't tracking a real risk difference; `/peer-review` in particular only runs when a peer already reviewed, making its "solo" branch vacuous. `Finding Categorization` collapsed from four buckets back to three (Auto-applicable / Needs sign-off / Needs human judgment); the Agent-resolvable bucket is gone. This also resolves `decouple-review-tooling.md` item 1. Correction to obs #3 itself: its "commands not skills" doc-model point is already resolved as of the `chore/retire-pair-flow-remnants` PR (#33, merged 2026-06-25) — current `CLAUDE.md` already says "The skills planwright supplies"; obs #3 was stale on that point even before this entry. The one thing obs #3 raises that's genuinely still open is `decouple-review-tooling.md`'s own "convergence layer" doc-framing point (that note's "Why it matters" #3 / "Scope" #3, not obs #3's own text, which never uses that phrase) — untouched by this branch.
