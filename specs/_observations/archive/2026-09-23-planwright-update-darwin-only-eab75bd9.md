- 2026-09-23 [dotfiles] The planwright plugin update tasks (marketplace update + plugin update) live in roles/osx/tasks/upgrade.yml, which only runs on Darwin, so `mise run upgrade` on the Linux host never updates planwright; the install moved to the cross-platform claude role but its update path did not.
Refuted: 2026-09-23. The Linux host does get planwright updates without the Ansible upgrade path: its plugin cache holds eight releases from 0.36.0 through 0.43.0 (0.37.0 and 0.38.0 were never cached), each picked up within days of publication, so something outside that playbook path (most likely Claude Code's own plugin updates, not confirmed) keeps it current.
