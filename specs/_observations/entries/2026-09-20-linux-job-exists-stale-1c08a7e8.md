- 2026-09-20 [dotfiles] services-declaration-test.sh's linux-job-exists assertion already fails on origin/main (unrelated to any single branch): it expects exactly one ubuntu-* runs-on job in .github/workflows/test.yml and now finds two (skill-contracts and the services-linux job pinned to ubuntu-26.04). Noticed while reviewing brewfile-managed-mac, which doesn't touch either job. Worth a follow-up to update the assertion or split it into a per-job check.
