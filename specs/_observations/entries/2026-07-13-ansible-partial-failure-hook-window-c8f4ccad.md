- 2026-07-13 [dotfiles] osx.yml's settings.json-merge task (hooks.Stop/Notification -> []) runs after network-dependent Claude Code CLI install/update tasks with no ignore_errors/failed_when guard. If those fail mid-playbook on a machine with Claude Code already configured, the OLD materialized ~/.claude/settings.json keeps pointing Stop/Notification hooks at scripts that were deleted in this branch (inbox-write.sh, notify-event.sh, inbox-session-start.sh), so every Stop/Notification event errors (missing-file) until the playbook is retried to completion. Same pattern for the analogous ~/.tmux/scripts symlink + a live tmux server holding the old @dracula-plugins string until reload/restart. Not unique to this branch -- inherent to any script-deletion-plus-later-materialized-config-update ordering in osx.yml. Surfaced during /self-review's error-handling lens on chore/remove-inbox-tmux-monitors. Consider reordering the settings.json-merge task earlier, or adding ignore_errors to the CLI install/update tasks, as a future hardening pass.
