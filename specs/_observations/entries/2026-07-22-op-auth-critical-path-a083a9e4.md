- 2026-07-22 [dotfiles] Task 3 media-server-cleanup: the parked risk-register precondition (authenticated op session for the playbook run) fired three times before succeeding. 1Password desktop-app CLI authorization is one-shot per op process with a ~60s window and no longer-lived approval option, so any playbook run containing claude-mcp-sync-github.sh needs a human watching for the prompt at the right moment; background/unattended runs cannot pass it. Candidate improvements for a future spec: move the MCP sync off the default playbook critical path (own tag), or document a pre-authorization step, or use a 1Password service account for automation contexts.
