- 2026-09-03 [dotfiles] 37 tracked files in this repo carry REQ/D-ID provenance in comments (257 lines), densest in the spec-verifying test scripts where the IDs are the test-case-to-requirement mapping the file exists to provide (scripts/services-declaration-test.sh header states this outright). The "A comment must earn its place" rule resolves the test case: it bans provenance in checked-in comments but carves out a test labelling which requirement it verifies, justified by the spec pinning that requirement to its verification path. Still open: whether the remaining non-test provenance comments (the Ansible task and defaults files, .gitleaks.toml) should be swept to match, which is its own change rather than part of the rule.
