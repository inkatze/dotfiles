- 2026-07-23 [dotfiles] orchestrate(linux-migration): the critical-path selector returned Task 2 (Pre-wipe machine prep) as the ready head, but Tasks 1-3 are human-only manual machine work (firmware, pmset, Recovery-mode Startup Security, backup) with no repo deliverable a subagent /execute-task worker can satisfy. Only Task 4 (repo work) was agent-dispatchable. The guard-infrastructure-first overlay covered this by hand, but there is no machine-readable signal distinguishing human-manual tasks from agent-executable ones. Consider a tasks.md task-class marker (manual|agent) the selector/backend can read so a subagent tower skips manual heads instead of relying on a per-step operator judgment call.
