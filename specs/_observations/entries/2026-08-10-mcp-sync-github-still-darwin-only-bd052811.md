- 2026-08-10 [dotfiles] scripts/claude-mcp-sync-github.sh is still invoked only from roles/osx (homebrew.yml and upgrade.yml), which is the same gap this branch just closed for the Gemini key sync: roles/claude is cross-platform and now configures Claude Code on Linux, so the Linux host has settings.json, commands and hook scripts but never gets its GitHub MCP server. It is also the one op-reading script with no service-account-token fallback, so moving it would need that added to work headlessly.
