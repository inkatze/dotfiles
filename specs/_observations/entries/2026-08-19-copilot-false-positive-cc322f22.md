- 2026-08-19 [dotfiles] Copilot claimed ansible get_url force:true always reports changed and would defeat idempotency; three consecutive measured runs (download task ok, play changed=0, extract skipped) refuted it, and its recommended force:false would have permanently skipped the download once cached, so a new upstream release would never be detected. Worth remembering that Copilot reasons about ansible changed-semantics from the flag name rather than the module contract.
