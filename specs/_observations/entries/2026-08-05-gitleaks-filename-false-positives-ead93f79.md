- 2026-08-05 [dotfiles] The .gitleaks.toml internal-hostname rule matches repo-owned FILENAMES, not just machine names: config.local, config.lan (from roles/ssh/files/config.lan.tpl) and planwright.local (from .claude/planwright.local.yml) all fire. The remote-shell kickoff hit this as a hard commit block - gitleaks git --staged exited 1 on 11 findings, every one a filename. The same class is already committed across ~20 tracked files (roles/ssh/README.md, both CLAUDE.md copies, roles/ssh/files/config, the whole specs/pair-flow bundle), so those commits either predate the rule or bypassed the hook. Resolved for this bundle with three anchored allowlist regexes, but the class will recur in any future bundle that names these files, and the allowlist grows one entry per filename. A rule-level fix (excluding known config-file extensions from the private-TLD alternation) would stop the recurrence; it was rejected here as out of scope for a spec kickoff and as risky regex surgery on a REQ-F1.1 backstop. Worth deciding deliberately rather than accumulating allowlist entries.
