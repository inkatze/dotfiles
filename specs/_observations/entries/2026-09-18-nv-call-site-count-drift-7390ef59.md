- 2026-09-18 [repo] Three places state how many tmux panes tm.fish sends the literal nv string to: the comment above alias nv in config.fish, the editor section of the repo CLAUDE.md, and an entry under specs/_observations. Nothing asserts they agree, so adding or removing a workspace function re-opens a three-way drift. A lefthook check deriving the count from grep -c "'nv' Enter" would close it.
