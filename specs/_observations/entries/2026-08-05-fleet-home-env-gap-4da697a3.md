- 2026-08-05 [dotfiles] Every fleet-*.sh script failed to resolve its cross-spec home when invoked from /orchestrate on this host: CLAUDE_PLUGIN_DATA is not exported into the Bash tool environment, so fleet-state.sh fell through to writer mode and looked for ~/.claude/planwright/plugin.json, which a marketplace install does not have. The real home exists at ~/.claude/plugins/data/planwright-planwright/fleet. Worked around by wrapping each call in a shim exporting CLAUDE_PLUGIN_DATA; unworked-around, fleet-throttle check exits 2 and a tower either fails closed or silently skips rate-limit governance.
