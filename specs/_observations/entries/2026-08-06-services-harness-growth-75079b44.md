- 2026-08-06 [dotfiles] scripts/services-declaration-test.sh is becoming the dev-services bundle every task adds to: Task 2 wrote it at roughly 300 lines, Task 3 took it past 700, and Tasks 4 and 6 both have assertions that belong in it. It is still one file interleaving bash cases with python heredocs, which was the right shape at 300 lines and is getting harder to read at 700 -- the grouped header comment is now the only map of what is in it. Nothing is wrong with it today and splitting it mid-bundle would create merge pain for the concurrent Task 4 worker, so this is a note for after the bundle lands rather than a change to make now. The natural seam if it is ever wanted is the one the header already draws: structural assertions over the declaration and task files, behavioural assertions that run ansible under stubs, and repo-wide hygiene assertions that shell out to git.
