- 2026-08-06 [dotfiles] scripts/gitleaks-rules-test.sh case 15 (committed-block-current) guards on '[[ -f $identifier_src ]]', but the assertion needs a source the generator can actually generate from, not merely one that exists. On this host the source file is present at mode 0600 and holds only comment lines, so the generator correctly refuses with its no-identifiers exit (3), and the harness reports 'committed block differs from a fresh generation; re-run --write' -- which names the wrong cause and points at a fix that cannot work. The accurate outcome is the visible skip the case already has a branch for. Suggested shape: gate on the generator succeeding (run it to /dev/null and branch on exit status) rather than on file existence, so an unusable source skips visibly instead of failing misleadingly. Surfaced during the specs/dev-services Task 2 convergence pass while running the sibling harness as a regression check; not fixed there because it is Task 1's artifact and Task 1 is parked in Awaiting input for the very operator action this message would misdirect.
