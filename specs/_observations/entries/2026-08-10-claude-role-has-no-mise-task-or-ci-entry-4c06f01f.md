- 2026-08-10 [dotfiles] roles/claude has neither a mise.toml task (there is no mise run claude) nor a CI matrix entry in .github/workflows/test.yml, which lists ssh git kitty tmux fish neovim osx environments services upgrade. That absence is why its two 1Password tasks had to borrow the osx and linux tags to stay reachable, making them the only place a platform tag names a task outside its platform role. A mise run claude task plus a matrix entry would remove the wart and give the repo cross-platform coverage of the role that carries the global CLAUDE.md, settings.json and hook scripts.
