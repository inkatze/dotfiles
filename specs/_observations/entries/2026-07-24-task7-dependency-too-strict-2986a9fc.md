- 2026-07-24 [dotfiles] Task 7 (Dotfiles stabilization loop) declares Dependencies: 4, 6 — but the work it describes has been proceeding for a whole session while Task 7 derives blocked/deps-unmet. PRs 55, 56, 57, 58 and 60 are all literally Task 7 deliverable text ("the repo playbook run against the Linux host, with every first-run failure fixed (repo-side or host-side) and each fix committed"): the config.fish brew guards, the gitconfig macOS-only SSH signer, the neovim macOS-only tarball, the apt/mise privilege split, and the tags:[always] brew --prefix that made the entire playbook unrunnable on Linux. None of those fixes needed anything from Task 6 (T2 hardware bring-up); they are pure repo-side cross-platform defects discoverable the moment a playbook is invoked on a Linux host. What genuinely depends on Task 6 is only the CONVERGENCE CRITERION — "two consecutive runs, zero failed, second reports changed=0" — which does want a fully brought-up machine so hardware-dependent roles are exercised. Effect of the current edge: real progress accumulates under chore branches with no Planwright-Task trailer, so the derivation shows Task 7 blocked and untouched while most of its substance is already merged, and the eventual Task 7 unit will be mostly bookkeeping over work already done. Options for the amendment: split Task 7 into a repo-side cross-platform fix unit (depends on 4 only) and a convergence unit (depends on 6), or relax the dependency and keep the convergence criterion as the gate. Not acted on — dependency edges are spec content and belong to /spec-kickoff or /spec-draft, not to an orchestration step.
