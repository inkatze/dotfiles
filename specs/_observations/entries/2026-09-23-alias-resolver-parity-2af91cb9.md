- 2026-09-23 [dotfiles] playbook.sh now refuses an alias that is not a plain name, but the /panel-review and /code-review resolver snippets do not: a file holding only "," resolves to a profile of "," there and falls to gemini. Nothing enforces parity between playbook.sh and the two snippets beyond prose (skill-contracts.sh compares the two snippets with each other only); a tripwire that greps playbook.sh for the same trim and non-empty guard would make the "same test" claim testable.
