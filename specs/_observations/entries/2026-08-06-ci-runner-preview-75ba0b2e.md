- 2026-08-06 [dotfiles] specs/dev-services D-7 pins the CI runner to `ubuntu-26.04`. Verified 2026-08-06 against actions/runner-images: the label exists and is usable, but its status is **public preview**, not GA (`ubuntu-latest` still resolves to 24.04). The bundle sources record it as "generally available under its own explicit label", which reads as GA and is at best ambiguous. Practical consequence: a preview image can be rebased or withdrawn on a shorter cycle than a GA one, and GitHub warns of capacity/queueing variance on it. The pin is still the right call (D-7s reasoning is unaffected), but the job may see flakiness attributable to the image rather than to the provisioning. Worth a look when 26.04 goes GA: nothing to change, but the reason for occasional oddness disappears.
