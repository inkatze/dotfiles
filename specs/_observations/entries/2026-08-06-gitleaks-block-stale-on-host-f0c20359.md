- 2026-08-06 [dotfiles] scripts/gitleaks-rules-test.sh fails its committed-block-current assertion on the Linux host: ~/.config/dotfiles/private-identifiers now exists (it did not when Task 1 of specs/dev-services ran, which is what that task Awaiting-input bullet records), but .gitleaks.toml has never been regenerated from it, so the committed rule block and a fresh generation differ. Unrelated to any branch that does not touch gitleaks; surfaced from the Task 5 wider-suite run. Fix is the one Task 1 already names: scripts/gitleaks-identifier-rules.sh --write, then commit the block. Worth noting that the Task 1 bullet now understates the state, since its stated precondition (the file is absent) no longer holds.
