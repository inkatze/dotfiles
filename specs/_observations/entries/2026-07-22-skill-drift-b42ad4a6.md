- 2026-07-22 [dotfiles] skill-drift(spec-draft): EnterWorktree fails under planwright fleet-worktree-track.sh hook-create — the harness WorktreeCreate payload carries no worktree_path field the script can extract, so the hook echoes nothing and the harness refuses creation; drafting degraded to raw git worktree add + EnterWorktree path=. The verified-contract comment in the script needs re-verification against the current hooks doc.
