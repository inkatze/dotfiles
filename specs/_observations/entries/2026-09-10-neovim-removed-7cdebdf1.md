- 2026-09-10 [dotfiles] neovim is removed, from the repo and from the Linux host; a replacement editor is expected later. Gone with it: `roles/neovim/` and its tracked config, the `mise run neovim` task, the CI matrix entry, the README and CLAUDE.md sections, the Brewfile's jdtls / lua-language-server / efm-langserver / stylua, the lua-language-server mise pin, and the editor-only default packages (`neovim` gem; `neovim`, `mcp-hub` and `vim-language-server` npm; `neovim`, `pynvim` and `msgpack` python). EDITOR, the `v` alias and git's core.editor moved to `vi` rather than `vim`: this host carries only vim.tiny, exposed as /usr/bin/vi, and macOS ships /usr/bin/vi too, so `vi` is the one name that resolves on both. Per the historical-record convention prior entries are left as written, but two are now moot — 2026-08-19-tooling-gap-a5ad218e (stylua's tab defaults disagreeing with roles/neovim's 2-space lua files; settled by the "drop the tool" branch of the choice it posed) and 2026-08-19-ci-idempotency-765be2e1 (the neovim role's CI entry could move to strict_idempotency: true) — and 2026-08-19-third-party-home-assumption-58c66feb (elixir-tools.nvim's unguarded vim.env.HOME) is no longer actionable here, being upstream in a plugin this repo no longer pins.
