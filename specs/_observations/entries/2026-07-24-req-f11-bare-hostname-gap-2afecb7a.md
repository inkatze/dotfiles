- 2026-07-24 [dotfiles] REQ-F1.1 says no real hostname is committed, but the migrated host name appears bare (undotted) in four tracked functional files: roles/fish/files/fish/config.fish (the sshc alias), roles/kitty/files/kitty/ssh.conf (hostname line), roles/fish/files/ollama.fish (client-env hostname pattern), and scripts/playbook.sh (PERSONALHOST). The gitleaks internal-hostname rule cannot catch these: it only matches dotted .local/.lan/.home/.internal/.corp forms, which is deliberate per .gitleaks.toml comments, so bare hostnames are structurally outside the backstop. Net effect: the requirement is broader than its enforcement, and the ssh-role cleanup (PR for chore/ssh-lan-config-sync) only closed the dotted cases. Deciding whether bare hostnames are in scope for REQ-F1.1 — and if so routing these four through the same op-inject mechanism — needs a spec-level call, not a silent edit.
