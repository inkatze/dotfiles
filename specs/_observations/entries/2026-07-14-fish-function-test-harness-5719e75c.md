- 2026-07-14 [dotfiles] No test harness exists anywhere in this repo for fish function *behavior* (only lint via lefthook's fish-lint hook, which proves a file parses, not that it works). tmux-offload.fish landed ~194 lines of nontrivial logic (argparse, tmux polling loops, JSON logging, mkdir-based locking) with zero CI exercise of that logic -- the fish role's CI test step only symlinks the file into place. Surfaced during /self-review of the tmux-offload branch (2026-07-15); the human chose to accept this gap for now rather than block on it. Worth spec material for a bats-style (or similar) harness that can drive fish functions non-interactively, especially ones that shell out to tmux.
