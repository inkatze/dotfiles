- 2026-09-10 [dotfiles] planwright skills that resolve the active spec through the status render cannot read three of this repo bundles: spec-status.sh refuses claude-context, metrics-baseline and pair-flow with "has no Format-version: line; refusing to guess the format (fail closed)". pair-flow is the one stored Active bundle, so the render cannot see it at all. The repo also has no `mise run status` task, which /self-review names as the alternative entry point. Net effect: brief detection degrades to the ask-or-proceed-brief-less arm on every run, and the Agent-resolvable bucket is unavailable for any pass on a non-planwright branch. Adding the Format-version line to those three bundles and a status task would restore it.
