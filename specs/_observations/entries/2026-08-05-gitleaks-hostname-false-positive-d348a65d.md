- 2026-08-05 [dotfiles] The repo internal-hostname gitleaks rule (regex matching <name>.local|lan|home|internal|corp) fires on the planwright repo-local override config filename, which contains a dotted .local segment but is a filename, not a hostname. Any planwright artifact (kickoff brief, spec, doc) naming that file is blocked at pre-commit. Surfaced during /spec-kickoff of specs/dev-services, worked around by rewording the brief rather than widening the allowlist, since the rule comment explicitly declines to allowlist dotted .local names. A durable fix would need a filename-shaped exemption that does not weaken the hostname backstop.
