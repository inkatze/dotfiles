- 2026-08-06 [dotfiles] The repo has no GitHub Actions schema linter. yamllint checks .github/workflows/test.yml as YAML, which catches indentation and quoting but knows nothing about Actions keys: a misspelled `runs-on`, a bad `uses` ref, or a step key in the wrong place is well-formed YAML and fails only at runtime, on a runner. That was tolerable while the file held one matrix; specs/dev-services Task 6 added a second job shape with its own env and step list, so the surface roughly doubled. `actionlint` is the obvious candidate and would slot into lefthook next to yamllint with a glob on .github/workflows/. Not done here: adding a linter is not Task 6s scope, and the mise-managed toolchain would need a new pinned tool.
