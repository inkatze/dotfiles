- 2026-08-05 [dotfiles] An unattended planwright worker session in this repo runs with LC_ALL=C, which makes ansible-lint abort before linting anything: ansible-config dump exits non-zero with "Ansible requires the locale encoding to be UTF-8; Detected None". mise run lint therefore fails for reasons unrelated to the diff under test, and passes cleanly when re-run with LANG/LC_ALL set to C.UTF-8. Observed while running the full CI guard for specs/dev-services Task 1, whose diff touches no YAML or Ansible content at all. Worth deciding where the fix belongs: the mise lint task could export a UTF-8 locale itself, or the worker environment could stop pinning LC_ALL=C. Left unfixed here because it is outside Task 1 scope and the repo-wide blast radius (every Ansible-running task) deserves its own decision.
