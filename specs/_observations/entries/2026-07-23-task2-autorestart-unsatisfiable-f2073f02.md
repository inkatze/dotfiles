- 2026-07-23 [dotfiles] linux-migration Task 2 done-when "pmset -g shows autorestart enabled" is UNSATISFIABLE on the target hardware (MacBook Pro 15,1, a portable). autorestart is a desktop-only pmset capability; on Mac notebooks `sudo pmset -a autorestart 1` exits 0 but the SMC silently ignores it, so the key never appears in `pmset -g custom` (verified on the machine 2026-07-23, firmware 23P5067, macOS 15.6). It is also a macOS setting that would not survive the wipe to Linux. Task 10 already anticipates this ("Linux-native fallback researched if the pre-wipe setting did not survive"). Amend Task 2: drop or soften the autorestart done-when clause and defer power-on-after-outage entirely to Task 10, so Task 2 does not require an impossible verification.
