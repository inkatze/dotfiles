- 2026-08-05 [dotfiles] The repo ships a growing number of bash scripts (scripts/, roles/claude/files/scripts/) but neither lefthook nor the CI workflow runs shellcheck, so shell defects are only caught when someone thinks to run it by hand. Running it ad hoc during the specs/dev-services Task 1 convergence pass immediately surfaced two real SC2209 warnings in new code, plus a pre-existing SC2015 in scripts/ssh-lan-config-sync.sh that nothing has ever flagged. shellcheck is available through mise (mise x shellcheck@latest), so wiring it as a lefthook pre-commit command globbed to *.sh, and/or a CI lint step, is cheap. Left undone here because it is a repo-wide guard change outside Task 1 scope and would surface pre-existing findings that need their own triage.
