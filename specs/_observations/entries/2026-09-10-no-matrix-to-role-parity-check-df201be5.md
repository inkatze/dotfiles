- 2026-09-10 [dotfiles] Nothing asserts that the CI matrix in .github/workflows/test.yml, the [tasks.*] entries in mise.toml, and the roles list in main.yml agree. The only parity assertions the repo ships are hardcoded to the services entry. Deleting a mise task while leaving its matrix entry fails loudly; deleting the matrix entry while leaving the role silently drops CI coverage and stays green. The neovim removal did all four sides by hand and was correct only for that reason. roles/claude and mise run linux are already-known instances of the same gap.
