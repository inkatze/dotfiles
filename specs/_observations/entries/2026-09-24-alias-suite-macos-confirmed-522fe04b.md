- 2026-09-24 [dotfiles] The 2026-09-23 alias-suite-macos-coverage risk materialized: on macOS, tr under a UTF-8 locale stripped an NBSP-only alias file to nothing, so playbook.sh fell back to work instead of refusing; fixed by trimming in the C locale (PR #165). The CI gap it names still stands: playbook-alias-test.sh runs only on ubuntu-latest, so a Mac regression is caught only by lefthook.
