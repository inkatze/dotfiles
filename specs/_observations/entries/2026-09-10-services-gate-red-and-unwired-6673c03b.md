- 2026-09-10 [dotfiles] scripts/services-declaration-test.sh fails on origin/main: linux-job-exists demands exactly one ubuntu-runs-on job and the skill-contracts job added alongside services-linux makes two. It is also wired into neither CI nor lefthook, so nothing runs it and the red went unnoticed. Its matrix-preexisting-unmodified assertion is separately over-broad: REQ-E1.4 is scoped to the dev-services bundle but the check is written as an unconditional "the diff of .github/workflows/ against origin/main may add lines and remove none", so it fires on any branch that edits CI for any reason. Decide whether the gate should run somewhere, be scoped to the services entry, or be retired.
