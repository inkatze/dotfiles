- 2026-09-10 [dotfiles] CLAUDE.md states the generated `private-project-identifier` rule block "is committed, so the guard still works on a fresh checkout and in CI", but the tracked .gitleaks.toml contains neither the BEGIN/END markers scripts/gitleaks-identifier-rules.sh writes nor any such rule, and no CI workflow runs gitleaks at all (lefthook pre-commit is the only invocation). So the guard that REQ-D1.4 hardened against silently matching fewer identifiers is currently matching none, on every checkout and in CI, and the doc says otherwise. Found while checking whether the neovim removal weakened a documented posture; it predates that branch.
