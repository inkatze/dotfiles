- 2026-07-24 [dotfiles] roles/git/files/gitconfig hardcodes gpg.ssh.program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign with commit.gpgsign = true. The git role symlinks this file to ~/.gitconfig on every host including Linux, where the 1Password signer lives at /opt/1Password/op-ssh-sign. On the migrated Linux host every commit will fail once the playbook runs (linux-migration Task 7 territory): needs an os_family-conditional signer path or a templated gitconfig.
