- 2026-07-22 [dotfiles] fleet-dispatch-guard check-launch (risk 20: demands --permission-mode/--settings in argv) and fleet-dispatch-worktree validate_launch_extra (escalation pin: refuses exactly those flags as extra launch args) are mutually unsatisfiable on the tmux rung when user settings pin defaultMode auto: check-inherited refuses (risk 19) and the primitive cannot construct a check-launch-passing argv. Observed dispatching media-server-cleanup task 1. The guard-passing argv exists (append --settings config/worker-settings.json; verified exit 0) but only a human-pasted launch can carry it. Candidate fixes: do_attach structurally appending the shipped worker-settings fragment (a trusted literal, not caller input), or the pin allowlisting --settings only when its value resolves to the plugin config/worker-settings.json.
