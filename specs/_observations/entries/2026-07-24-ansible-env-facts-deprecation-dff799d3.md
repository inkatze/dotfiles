- 2026-07-24 [dotfiles] ansible-core 2.21 deprecates INJECT_FACTS_AS_VARS defaulting to True; top-level fact injection is removed in ansible-core 2.24. Reproduced on the Linux host with a minimal fact-gathering play: "[DEPRECATION WARNING]: INJECT_FACTS_AS_VARS default to True is deprecated, top-level facts will not be auto injected after the change. This feature will be removed from ansible-core version 2.24." The repo relies on this pervasively: 93 usages across 17 role files, 57 of ansible_env.HOME and 36 of ansible_env.PWD, including every symlink task that anchors dotfiles at the checkout path. Nothing breaks today and the syntax check is silent (the warning only appears on a fact-gathering run), so this will stay invisible until an ansible upgrade breaks every role at once. Migration is mechanical (ansible_env.X -> ansible_facts.env.X) but touches most of the repo, so it wants its own unit rather than riding along with unrelated work. Surfaced by moving to a current user-level ansible; apt resolute ships an older core that does not warn.
