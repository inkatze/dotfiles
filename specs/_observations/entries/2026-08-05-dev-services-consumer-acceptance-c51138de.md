- 2026-08-05 [dotfiles] Operator named the real acceptance test for the dev-services bundle during its first orchestration step: running the sibling tecpan project over TLS bound to 0.0.0.0 against the provisioned services, with no issues. That is deliberately outside this bundle (tasks.md puts both Remote reachability of either service and Consuming-project configuration in Out of scope, and both services bind loopback), so no task Done-when covers it and no worker will satisfy it. Seed for a successor bundle: either a consuming-project connection spec or the remote-shell/access layer already spun out, whichever ends up owning TLS termination and non-loopback exposure.
