- 2026-08-06 [dotfiles] The repo now ships four assertion harnesses under scripts/ (services-declaration-test.sh, gitleaks-rules-test.sh, ssh-lan-config-sync-test.sh, postgresql-access-test.sh) plus stale-declarations-test.sh from specs/dev-services Task 5, and there is no single command that runs them: mise run lint is yamllint + ansible-lint + syntax-check only, lefthook pre-commit runs neither, and the CI test job runs roles rather than scripts. Each script individually justifies being manual, but the aggregate is that a growing body of assertions runs only on the branch that wrote it. Two candidate shapes: a mise run test task that runs the harnesses whose preconditions are present and skips the rest visibly, or a CI job that runs the checkout-only ones. Surfaced during Task 5, whose harness has no independent CI signal behind it at all, unlike the siblings where CI runs the role or the scanner itself.
