- 2026-08-05 [dotfiles] check-obs.sh fails with "unexpected path directly under the observations root: decouple-review-tooling.md". The file sits at specs/_observations/decouple-review-tooling.md rather than under entries/. Pre-existing, unrelated to the remote-shell bundle, and it makes the checker exit non-zero for every run until it is moved or the checker is taught about it. Surfaced during the remote-shell kickoff tool-grounded discovery pass; not acted on, since the kickoff does not own it.
