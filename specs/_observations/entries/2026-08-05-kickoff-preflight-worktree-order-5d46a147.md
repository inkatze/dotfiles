- 2026-08-05 [planwright] /spec-kickoff cannot run from the main checkout when the spec bundle lives only on its own spec branch: pre-flight steps 1-4 (identifier, bundle+mode, validator, config) all complete, then step 5 detects the existing spec worktree, prints `claude --worktree <spec>-spec`, and stops — so that work is spent and re-run after the context switch. Two rough edges. (a) The bundle is invisible from the main checkout: specs/<spec>/ does not exist there until the spec PR merges, so step 2 reads as "missing file → structural defect, point at /spec-draft" when the truth is "wrong working location". The skill has to find the worktree before it can trust its own bundle check, which inverts the documented step order. (b) The working-location resolution is step 5, after the validator has already run; resolving location first would make the stop cost nothing. Observed on specs/remote-shell (all four files Draft, format-version 2, validator 0/0, no kickoff-brief.md) while the bundle sat unmerged on planwright/remote-shell/spec.
