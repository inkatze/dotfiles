- 2026-08-19 [dotfiles] The test (osx) and test (upgrade) CI jobs have been hitting the 30-minute timeout on every run since at least 2026-08-15, on main and every branch, always hanging on "osx : Install common packages from Brewfile". Ansible captures brew bundle stdout so the log shows nothing after the task header. Two red checks on every PR train people to ignore CI; either raise the timeout, stream the brew output, or split the Brewfile install into smaller tasks.
