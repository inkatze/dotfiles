- 2026-08-06 [dotfiles] The test (osx) and test (upgrade) CI jobs are cancelled by the 30-minute timeout-minutes cap on every run, on main as well as on PR branches: the last four runs on main show both jobs cancelled, and PR #104 reproduces it. Both stall in "Install common packages from Brewfile" with no error, then get cancelled mid-step; the idempotency check that follows never runs, so those two roles have had no passing CI signal for some time and the red checks read as a branch failure to anyone opening a PR. Not a flake a retry clears, so retrying costs macOS runner minutes (10x multiplier) to re-derive a known answer. Candidate fixes, in rough order of cost: raise timeout-minutes for those two entries only; split the Brewfile install into its own cached step; or trim what the CI Brewfile installs, since the matrix does not need the full desktop set. Surfaced during specs/dev-services Task 5, whose own relevant checks (lint, test (services) at strict idempotency) both pass.
