- 2026-09-10 [dotfiles] specs/linux-migration is Ready and its test-spec pins REQ-D1.2 to a [manual] check comparing a macOS playbook run against specs/linux-migration/baseline/pre-split-macos-baseline.md. That baseline lists eight `neovim :` task lines and asserts a 59-task count, so after the neovim removal an operator running REQ-D1.2 sees a large unexplained delta. The baseline is a dated capture and should not be rewritten; the delta needs recording where the REQ-D1.2 runner will see it, which is a spec amendment rather than a branch edit.
