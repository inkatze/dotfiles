- 2026-08-06 [dotfiles] The services role's only macOS action is symlinking roles/services/files/my.cnf, which configures MySQL 8.0 and MariaDB 10.6 clients (sockets under /tmp, a hardcoded port 3307, Homebrew data/log paths under /opt/homebrew/var/mariadb). Surfaced while guarding that task to Darwin for specs/dev-services Task 2: nothing in the repo installs either engine (they are absent from every Brewfile), so this may be config for services no Mac still has. The new services CI matrix entry will now exercise the symlink on a runner, which proves the task converges but says nothing about whether its content is still live. Worth a look when someone next touches the role: either the engines belong in a Brewfile or the file and its task are dead weight. Deliberately not folded into Task 5, whose REQ-B1.5 scope is the stale out-of-scope assertions in roles/linux/defaults and the repo guide, not this file.
