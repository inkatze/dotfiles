- 2026-08-05 [dotfiles] fleet-dispatch-guard.sh check-launch refuses the exact launch shape fleet-streamjson.sh pins: that shape carries --permission-prompt-tool stdio but no --permission-mode and no --settings, and the guard treats absence of an explicit non-auto mode source as a refusal (risk 20). The dispatching skill has to append --settings config/worker-settings.json itself for the two to agree, which is an undocumented coupling between the guard and the stream-json rung rather than something either script states. Worth either pinning --settings inside fleet-streamjson.sh launch or naming the requirement in the orchestrate skill dispatch section.
