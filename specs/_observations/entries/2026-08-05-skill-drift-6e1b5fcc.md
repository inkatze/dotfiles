- 2026-08-05 [dotfiles] skill-drift(spec-kickoff): the ready-flip refusal arm and the format-version 2 tasks.md contract disagree about what may sit under "## Awaiting input". kickoff-verification (Terminal ready-flip CI gate) and SKILL.md step 8 both direct a failed ready-flip to be recorded as an Awaiting-input entry naming the pending flip. But spec-format Format-version 2 says that section "holds reference bullets only" - bullets whose bolded lead is exactly **Task <id>**, naming an existing task block - while Deferred and Out-of-scope may also hold plain non-task bullets. A pending spec-PR ready-flip is a bundle-level publication step with no task id, so it cannot be expressed as a reference bullet without falsely parking a real task (which would also distort the derived status render). Hit for real on the remote-shell kickoff 2026-08-05: CI was not green, the refusal arm fired, and the entry had to be written as a non-task bullet. spec-validate.sh accepted it with 0 errors at Ready, so the constraint is documented but unenforced, and the anchor was unaffected as designed. Either the v2 contract should permit plain bullets in Awaiting input for bundle-level blockers, or the refusal arm should name a different home. Not resolved here; /spec-draft owns folding drift into an amendment.
