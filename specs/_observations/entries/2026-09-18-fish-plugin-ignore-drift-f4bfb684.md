- 2026-09-18 [repo] roles/fish/files/fish/functions/ mixes repo-owned functions with fisher-installed ones, so it carries per-name ignore rules (functions/_*, fisher.fish, now bass.fish) while conf.d, completions, themes and fish_variables are ignored wholesale. Each new plugin that installs a function without a leading underscore needs another line, and nothing reports the gap until git surfaces the file as untracked or a git add -A sweeps it into a commit.
