- 2026-09-03 [dotfiles] skill-contracts-test.sh failed once, reporting `FAIL retired-bucket: expected message containing "retired Agent-resolvable bucket", got: ERROR: panel-review.md references the retired Agent-resolvable bucket`, an assertion that rejected output plainly containing the expected fragment. Not reproduced in 11 consecutive re-runs on an unchanged tree, and the branch that saw it touches no command file. Recorded as a first data point: this script is a CI gate, so a nondeterministic failure there is a red build nobody can attribute.
