- 2026-08-06 [dotfiles] The user-global CLAUDE.md "Finding Categorization" section defines three buckets (Auto-applicable, Needs sign-off, Needs human judgment) and instructs skills to present "three tables in fixed order", while the planwright doctrine the same skills resolve at run time (doctrine/finding-categorization.md, 0.31.0) defines four, inserting Agent-resolvable between the first two and requiring four tables. Both are in context during a planwright run, so a review skill is following two different taxonomies at once; the dotfiles copy predates the plugin extraction and was not updated when Agent-resolvable was added. Noticed while routing findings during /polish on specs/dev-services Task 4. The fix is a dotfiles edit, not a planwright one — either restate the section to match the four buckets or replace it with a pointer to the plugin doctrine so it cannot drift again.
